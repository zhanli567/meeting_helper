# 变更记录

本项目采用语义化版本和 Git 提交共同记录变更。所有功能性变更、修复和重要工程调整都应在这里留下记录。

## [未发布]

### 新增

- 初始化 Git 仓库。
- 使用 Vue、TypeScript、Vite、Pinia 和 Vue Router 创建前端工程。
- 使用 Spring Boot、Spring Data JPA、PostgreSQL、H2 和 Bean Validation 创建后端工程。
- 建立场馆模板、会议布局快照、参会人员、颁奖记录、排座方案、方案版本和多目标临时占用模型。
- 提供排座工作台、人员移动与交换、锁定、手工新增和软删除接口。
- 提供元数据驱动的通用会议与颁奖会议Excel导入模板、重复工号预览和导入提交接口。
- 提供Excel排座图、座位明细、人员名单、待排名单及PDF场馆图导出。
- 增加包含舞台、门、走廊、九排座位、双格座席、摄像机和禁用末排的演示数据。
- 增加后端集成测试，覆盖工作台数据、人工排座、导入模板及导出文件。
- 增加 Vue 人工排座工作台：场馆缩放、人员拖入空座、已排人员换座、移回待排、座位锁定和本次会话撤销/重做。
- 增加可搜索、筛选、排序、分组并保存视图的待排人员面板，支持连续排座。
- 增加导入预检与重复工号选择、单人新增、人员详情、方案版本保存、Excel/PDF 导出等交互。
- 增加场馆模板库和二维网格场馆设计器，支持跨行跨列元素、批量生成座位及座位编号。
- 增加 Element Plus 按需引入、路由懒加载和前端单元测试。
- 增加架构说明、本地运行步骤和第一版能力边界文档。
- 增加系统首页和“我的会议”入口，并建立可替换的当前用户/租户会话边界，为后续统一认证和数据隔离预留接入点。
- 增加待排人员分页、元数据驱动的通用排序及独立滚动区域，避免人员过多时带动整个页面滚动。
- 增加排座拖拽目标提示、空白画布抓取平移，以及“保存筛选方案”“连续排座”的明确交互说明。
- 增加方案版本历史列表和快照恢复能力，并以集成测试覆盖保存、修改和恢复闭环。
- 增加场馆设计器拖动绘制、框选擦除、画布平移和元素旁浮动属性编辑器。
- 增加 DDL 规范测试，自动拦截根目录 `DDL` 之外的 SQL、非建表语句和缺少 `t_` 前缀的表名。
- 增加自定义场馆编辑与软删除能力，并在前后端同时校验场馆名称和会议名称是否重复。
- 增加场馆设计器框选后选择元素、滚轮缩放、空白区域拖拽平移、撤销/重做、双击删除和元素属性确认/取消交互。
- 增加可拖动、可自动缩边的人员添加入口，统一提供单人添加、Excel模板下载和Excel批量导入。
- 增加“草稿—发布版本”工作流：草稿可编辑和发布，V1/V2 等已发布快照只读且可独立导出。
- 增加已发布版本快照读取与按版本导出接口，并以集成测试覆盖快照不可变、场馆管理和名称冲突。

### 修复

- 工作台的锁定状态改为读取当前排座方案中的座位锁定信息。
- 将人员扩展属性自动加入工作台筛选字段，避免导入新场景字段后必须修改前端代码。
- 排座接口失败时不再写入撤销/重做历史。
- 修复场馆设计器只能在新增前设置元素宽高、橡皮擦反馈不明确和属性面板需要长距离滚动的问题。
- 修复拖动人员时座位缺少落点提示、版本保存后无法查看和恢复的问题。
- 修复工作台、场馆库和设计器使用浏览器全页滚动导致主要操作区移出视口的问题。
- 修复本地启动时数据库密码环境变量缺失或错误导致 PostgreSQL 认证失败的问题。
- 修复场馆库滚动条出现在页面边缘、场馆卡片无法删除或编辑的问题。
- 修复场馆元素编号过长溢出、编号覆盖名称、颜色修改无法预览回退和属性卡无法主动关闭的问题。
- 修复工作区高倍缩放后画布无法继续拖动或滚动，以及人员拖放落点不明确的问题。
- 修复待排人员过多时无法分页或滚动到底部的问题，搜索调整为姓名模糊匹配和工号精确匹配。
- 修复自动缩边的人员添加按钮中心移出视口、短按被误判为拖动的问题。
- 修复前端迁移为 JavaScript 后组件运行时属性声明丢失，导致进入会议工作区时场馆画布和人员面板渲染异常、页面空白的问题。
- 补全场馆画布、人员列表、人员详情、单人添加、批量导入和版本弹窗的运行时属性及事件声明。
- 修复真实鼠标点击人员添加按钮时，轻微位移被误判为拖动而无法打开添加菜单的问题。
- 修复排座工作区缺少明确首页入口的问题，在顶部增加可直接返回系统首页的按钮。
- 修复“继续最近会议”始终进入会议列表第一项的问题，按当前用户记录并恢复最后访问的会议。

### 变更

- 正式运行数据库由文件型 H2 切换为 PostgreSQL，连接信息通过环境变量提供。
- H2 调整为仅供自动化测试使用，正式运行数据统一保存到 PostgreSQL。
- 将迁移脚本中的大文本列改为 PostgreSQL 与 H2 均支持的 `text` 类型。
- 将人员扩展属性和方案快照的实体映射同步调整为 `text`，保持 Hibernate 校验与迁移结构一致。
- 系统主色调整为蓝白色，舞台和业务分类色改用低饱和浅色。
- 将写死的“批次升序、职级降序”改为按后端字段定义生成排序选项；非颁奖会议不再显示批次排序。
- 将工作台顶部统计改为横向大字号展示，并将“保存当前视图”更名为含义明确的“保存筛选方案”。
- 后端正式配置和测试配置统一改用 YAML；本地 PostgreSQL 连接信息直接写入配置，不再依赖数据库环境变量。
- 数据库脚本统一为根目录 `DDL/meeting_helper.sql`，Maven 构建时复制给 Spring Boot SQL 初始化器。
- 10 张业务表全部改用 `t_` 前缀，初始化脚本仅保留可重复执行的 `CREATE TABLE IF NOT EXISTS` 语句。
- 移除不再需要的 Flyway 依赖和历史表，避免固定 DDL 文件名无法被识别。
- 删除本地遗留的 H2 文件数据库和临时 PostgreSQL 备份目录，并重新创建 UTF-8 的 `meeting_helper` 数据库。
- 场馆设计器移除固定左侧绘制工具栏，场馆信息改为可拖动、可吸附、可收起的悬浮面板，画布获得完整可用空间。
- 工作区移除写死的筛选、排序、保存筛选方案和连续排座入口，仅保留通用分组与搜索。
- 会议与版本选择合并到同一顶部上下文；品牌区作为返回首页入口，导入能力并入人员添加入口。
- 元素属性卡同时展示编号和名称，限制输入长度并提供蓝白色系常用色板；颜色在确认前仅作实时预览。
- 前端单元测试环境由 `jsdom` 切换为公司仓库可用的 `happy-dom@20.9.0`，并清理 `whatwg-url` 依赖链。
- 前端源码和构建配置由 TypeScript 全量迁移为 JavaScript，移除 `vue-tsc`、TypeScript、Pinia、Vitest、代码检查、自动导入和开发工具依赖。
- Vue、Vue Router、Element Plus、Axios、Vite 等公共依赖精确固定为公司 `eval_system` 已验证的低版本基线。
- 排座工作区状态改用 Vue 原生响应式单例管理，在移除 Pinia 后保持原有跨页面状态和交互方式。
- 不再提交 `frontend/package-lock.json`，并通过精确依赖版本保证无锁文件安装时的版本稳定性。
- 后端目录对齐公司 `eval_system` 的领域分层规范，按业务模块拆分 `api/dto`、`entity`、`repository`、`service`，并将公共实体与异常分别归入 `common/entity` 和 `common/exception`。
- 将会议、人员、场馆、排座版本及导入接口的请求/响应 DTO 从 Service 内部类型拆为独立文件，解除接口契约与业务实现的嵌套耦合。
- 后端改用开发环境统一安装的 Maven 命令运行和测试，不再保留仓库内的 Maven Wrapper 启动脚本。
- 草稿中支持双击已排人员或将其拖到右侧人员面板以移回待排列表，并提供明确的拖放落点反馈。
- 已发布版本的人员面板只展示全部人员，同时保留姓名、工号搜索和通用分组能力。
- 发布版本前由前后端共同校验待排人数，仍有未排人员时阻止发布并显示待处理人数。
- 前端依赖版本统一增加 `~` 范围，允许在相同次版本内获取兼容的补丁更新。

# 变更记录

本项目采用语义化版本和 Git 提交共同记录变更。所有功能性变更、修复和重要工程调整都应在这里留下记录。

## [未发布]

### 新增

- 初始化 Git 仓库。
- 使用 Vue、TypeScript、Vite、Pinia 和 Vue Router 创建前端工程。
- 使用 Spring Boot、Spring Data JPA、Flyway、H2 和 Bean Validation 创建后端工程。
- 建立场馆模板、会议布局快照、参会人员、颁奖记录、排座方案、方案版本和多目标临时占用模型。
- 提供排座工作台、人员移动与交换、锁定、手工新增和软删除接口。
- 提供元数据驱动的通用会议与颁奖会议Excel导入模板、重复工号预览和导入提交接口。
- 提供Excel排座图、座位明细、人员名单、待排名单及PDF场馆图导出。
- 增加包含舞台、门、走廊、九排座位、双格座席、摄像机和禁用末排的演示数据。
- 增加后端集成测试，覆盖工作台数据、人工排座、导入模板及导出文件。
- 增加 Vue 人工排座工作台：场馆缩放、人员拖入空座、已排人员换座、移回待排、座位锁定和本次会话撤销/重做。
- 增加可搜索、筛选、排序、分组并保存视图的待排人员面板，支持连续排座。
- 增加导入预检与重复工号选择、单人新增、人员详情、方案版本保存、Excel/PDF 导出等交互。
- 增加场馆模板库和二维网格场馆设计器，支持跨行跨列元素、批量生成座位及座位编号。
- 增加 Element Plus 按需引入、路由懒加载和前端单元测试。
- 增加架构说明、本地运行步骤和第一版能力边界文档。
- 增加系统首页和“我的会议”入口，并建立可替换的当前用户/租户会话边界，为后续统一认证和数据隔离预留接入点。
- 增加待排人员分页、元数据驱动的通用排序及独立滚动区域，避免人员过多时带动整个页面滚动。
- 增加排座拖拽目标提示、空白画布抓取平移，以及“保存筛选方案”“连续排座”的明确交互说明。
- 增加方案版本历史列表和快照恢复能力，并以集成测试覆盖保存、修改和恢复闭环。
- 增加场馆设计器拖动绘制、框选擦除、画布平移和元素旁浮动属性编辑器。

### 修复

- 工作台的锁定状态改为读取当前排座方案中的座位锁定信息。
- 将人员扩展属性自动加入工作台筛选字段，避免导入新场景字段后必须修改前端代码。
- 排座接口失败时不再写入撤销/重做历史。
- 修复场馆设计器只能在新增前设置元素宽高、橡皮擦反馈不明确和属性面板需要长距离滚动的问题。
- 修复拖动人员时座位缺少落点提示、版本保存后无法查看和恢复的问题。
- 修复工作台、场馆库和设计器使用浏览器全页滚动导致主要操作区移出视口的问题。
- 修复本地启动时数据库密码环境变量缺失或错误导致 PostgreSQL 认证失败的问题。

### 变更

- 正式运行数据库由文件型 H2 切换为 PostgreSQL，连接信息通过环境变量提供。
- H2 调整为仅供自动化测试使用，并增加 PostgreSQL 对应的 Flyway 数据库支持。
- 将迁移脚本中的大文本列改为 PostgreSQL 与 H2 均支持的 `text` 类型。
- 将人员扩展属性和方案快照的实体映射同步调整为 `text`，保持 Hibernate 校验与迁移结构一致。
- 系统主色调整为蓝白色，舞台和业务分类色改用低饱和浅色；新增 Flyway 迁移同步更新既有演示场馆。
- 将写死的“批次升序、职级降序”改为按后端字段定义生成排序选项；非颁奖会议不再显示批次排序。
- 将工作台顶部统计改为横向大字号展示，并将“保存当前视图”更名为含义明确的“保存筛选方案”。
- 后端正式配置和测试配置统一改用 YAML；本地 PostgreSQL 连接信息直接写入配置，不再依赖数据库环境变量。
